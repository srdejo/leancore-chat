# Tasks

## 1. Andamiaje e infraestructura

- [x] 1.1 Crear `backend/` (Gradle, Spring Boot 4.1, Java 26, paquete `com.leancore.chat`) con webflux, data-r2dbc, `r2dbc-postgresql`, validation, Lombok, MapStruct, springdoc-webflux y el starter de Anthropic de Spring AI (fijar la versión 2.x compatible con Boot 4.1 vía BOM `spring-ai-bom`), más reactor-test y Testcontainers PostgreSQL para test, usando el layout hexagonal de la referencia; verificar que `./gradlew build` compila y que la app arranca con `ANTHROPIC_API_KEY` vacío
- [x] 1.2 Añadir `docker/java-service.Dockerfile` (multi-stage con jlink, adaptado de `pragma/03-reto-reactivo`), `docker-compose.yml` con `postgres:17` (5432 publicado, volumen `pg-data`, healthcheck) y `backend` (8080, `ANTHROPIC_API_KEY`/`ANTHROPIC_MODEL` desde `.env`), `.env.example` sin secretos y `.env` en `.gitignore`; verificar con `docker compose up -d postgres` que se conecta desde el host con `psql -h localhost -U chat chat_db`
- [x] 1.3 Crear el workspace `frontend/` con Angular 22 (standalone, zoneless, Vitest) y los proyectos `chat-core` (librería), `user-app` y `admin-app`; llevar a `chat-core/styles/tokens.css` las fuentes (Instrument Sans, Instrument Serif, JetBrains Mono) y la paleta de `leancore-fintech`, e importarlas en ambas apps; verificar que `npx ng build user-app`, `npx ng build admin-app` y `npx ng test --watch=false` pasan
- [x] 1.4 Añadir `frontend/Dockerfile` (`ARG APP`) + `nginx.conf` (SPA fallback, proxy `/api` y `/ws` con `Upgrade`/`Connection` y `proxy_read_timeout` alto) y los servicios `user-app` (4200) y `admin-app` (4300) en compose; verificar que `docker compose up --build` sirve ambas apps en sus puertos

## 2. Persistencia y conversaciones (backend)

- [x] 2.1 Crear `schema.sql` idempotente con `bot_scope` (con la versión 1 de bicicletas por `insert ... on conflict do nothing`), `conversation` (incluye `last_sender_role`, `last_preview`) y `message` (incluye `reply_to_seq` y `bot_scope_version`; únicas `(conversation_id, seq)` y `(conversation_id, client_message_id)`; check `CUSTOMER|BOT|SYSTEM`; índices) y `spring.sql.init.mode=always`; verificar que las tablas existen al arrancar contra el Postgres de compose
- [x] 2.2 Modelar el dominio (`ConversationModel`, `MessageModel`, `SenderRole`, `ChatEvent`), las excepciones y los puertos `IConversationServicePort`, `IConversationPersistencePort` e `IMessagePersistencePort`; verificar que `domain` compila sin dependencias de Spring ni de infraestructura
- [x] 2.3 Implementar `ConversationUseCase` (crear con nombre de 1 a 60 caracteres, obtener, listar por actividad con extracto del último mensaje, historial con `afterSeq` y `limit` por defecto 100 y máximo 500) con tests unitarios en `StepVerifier` que cubran la validación, el "no encontrado" y los límites
- [x] 2.4 Implementar los adaptadores R2DBC (entidades, mappers MapStruct, repositorios) y verificar con un test de integración en Testcontainers que crear, listar ordenado y leer con `afterSeq` devuelven lo esperado
- [x] 2.5 Exponer `ConversationRestController` (`POST /api/v1/conversations` → 201, `GET /api/v1/conversations`, `GET /{id}`, `GET /{id}/messages?afterSeq=&limit=`) con `ControllerAdvisor` (400/404) y Swagger; verificar con tests de `WebTestClient` y en `/swagger-ui.html`

## 3. Envío ordenado e idempotente (backend)

- [x] 3.1 Definir `IMessageServicePort.send(conversationId, sender, clientMessageId, content, replyToSeq)` que devuelve `SendResult(message, duplicate)`, con validación de contenido de 1 a 2000 caracteres; verificar con un test unitario del caso inválido
- [x] 3.2 Implementar en el adaptador R2DBC la transacción de D2 (`UPDATE conversation ... RETURNING last_seq`, que también actualiza el extracto, + `INSERT message`) y la ruta idempotente de D3 (buscar primero y, ante violación de la restricción única, rollback y releer); verificar con Testcontainers que 50 envíos concurrentes producen `seq` 1..50 sin huecos y que el mismo `clientMessageId` enviado 5 veces en paralelo deja una sola fila con el mismo `seq` en los 5 resultados
- [x] 3.3 Implementar `MessageUseCase`: publicar en `IMessageBroadcastPort` solo después de persistir y nunca en duplicados, y encolar al bot solo los mensajes `CUSTOMER` nuevos; verificar con tests unitarios (puertos mockeados) que un duplicado no publica ni encola y que un fallo de persistencia no publica
- [x] 3.4 Implementar `InMemoryMessageBroadcastAdapter` (un sink multicast por conversación, `ChatEvent` = mensaje o `TYPING`, emisión concurrente segura y limpieza sin suscriptores); verificar con `StepVerifier` que dos suscriptores reciben el evento y que otra conversación no lo recibe

## 4. Canal WebSocket y reanudación (backend)

- [x] 4.1 Definir los DTO de frames (`SEND`, `MESSAGE`, `ACK`, `SYNCED`, `TYPING`, `ERROR`) y su serialización JSON; verificar con un test de serialización ida y vuelta
- [x] 4.2 Implementar `ChatWebSocketHandler` en `/ws/conversations/{id}` con `role` (`CUSTOMER`/`OBSERVER`), `name` y `lastSeq` del handshake, que cierra con código de error si la conversación no existe o los parámetros son inválidos, procesa `SEND` → `ACK`/`ERROR` y rechaza `SEND` de `OBSERVER` con `READ_ONLY`; verificar con tests de integración usando `ReactorNettyWebSocketClient`
- [x] 4.3 Implementar la reanudación de D5 (suscribirse al stream en vivo con buffer, luego historial `seq > lastSeq` con máximo 500, `SYNCED`, y lo vivo filtrado por `highWater`, dejando pasar los `TYPING`); verificar con un test de integración que un mensaje aceptado durante el reenvío llega exactamente una vez y en orden, que un participante que reconecta con `lastSeq=10` recibe 11..13 y después lo vivo, y que no recibe `TYPING` antiguos
- [x] 4.4 Añadir un ping cada 25 s a cada sesión y el cierre limpio; verificar que una sesión sin tráfico sigue abierta más de 60 s a través del nginx de compose

## 5. Configuración del alcance del bot (backend)

- [x] 5.1 Modelar `BotScope` y los puertos `IBotScopeServicePort` (`getActive`, `save`, `history`) e `IBotScopePersistencePort`, e implementar `BotScopeUseCase` con la validación de D14 (tema de 1 a 60, descripción hasta 500, 1 a 20 subtemas de 1 a 60 recortados y sin duplicados, negativa de 1 a 300, sin caracteres de control); verificar con tests unitarios de cada regla
- [x] 5.2 Implementar el adaptador R2DBC de `bot_scope` (activa = `max(version)`, insertar `max + 1` con un reintento ante choque de PK, historial descendente); verificar con Testcontainers que la versión 1 existe al arrancar y que dos guardados concurrentes producen dos versiones consecutivas distintas
- [x] 5.3 Exponer `BotScopeRestController` (`GET /api/v1/bot-scope`, `PUT /api/v1/bot-scope` → 201 con la versión nueva, `GET /api/v1/bot-scope/versions`) con 400 de validación y Swagger; verificar con tests de `WebTestClient`

## 6. Bot de soporte con Spring AI: Claude y OpenAI de respaldo (backend)

- [x] 6.1 Definir `IBotAnswerPort.answer(scope, history, question): Mono<BotAnswer>` y `BotAnswer(inScope, reply)` en el dominio, e implementar `BotReplyUseCase`: `TYPING(true)`, lectura de la versión activa del alcance al empezar, historial de los últimos 20 mensajes (sin `SYSTEM`), mapeo de D8 (fuera de alcance o no interpretable → `scope.refusalMessage`; recorte a 2000; error, timeout o deshabilitado → mensaje `SYSTEM` de no disponible), `send` con `clientMessageId` UUID v5 derivado de conversación + `seq`, `replyToSeq` y `botScopeVersion`, y `TYPING(false)`; verificar con tests unitarios de cada rama del mapeo, de que se registra la versión usada y de que una segunda generación del mismo mensaje no crea otra fila
- [x] 6.2 Implementar la cola del bot: serializada por conversación (en orden de `seq`), con concurrencia global de llamadas al LLM limitada y liberación de las colas inactivas; verificar con `StepVerifier` y un `IBotAnswerPort` con latencia simulada que tres mensajes seguidos se responden en orden y que una conversación lenta no retrasa a otra
- [x] 6.3 Implementar el adaptador de LLM genérico `LlmBotAnswerAdapter` (`infrastructure/out/ai`, una instancia por proveedor) con `ChatClient`, la plantilla fija `prompts/scoped-support.st` completada con `topic`, `description` y `subtopics` del `BotScope` (valores delimitados como datos), salida estructurada `BotAnswer`, temperatura 0.2, `max_tokens` 400, `boundedElastic` y timeout de 20 s, más el modo deshabilitado cuando `ANTHROPIC_API_KEY` está vacío; verificar con tests del adaptador (`ChatModel` falso) que el prompt armado contiene el tema, los subtemas y siempre las reglas fijas, que un valor con saltos de línea o "ignora lo anterior" queda delimitado, que el JSON inválido se traduce a "no interpretable" y que sin llave responde "deshabilitado" sin llamada de red
- [x] 6.4 Implementar la recuperación de mensajes del cliente sin respuesta (por `reply_to_seq`) al arrancar (conversaciones con actividad en las últimas 24 h) y al conectarse alguien a una conversación; verificar con un test de integración que un mensaje `CUSTOMER` sin respuesta recibe exactamente una respuesta tras reiniciar el contexto
- [x] 6.5 Test de integración de orden con el bot: con un `IBotAnswerPort` falso que tarda 500 ms, el cliente envía otro mensaje durante la generación; verificar que el cliente y un `OBSERVER` reciben la misma secuencia y que cada mensaje del cliente tiene exactamente una respuesta con su `reply_to_seq`. Además, un `PUT /api/v1/bot-scope` entre dos preguntas hace que la segunda respuesta registre la versión nueva y la primera conserve la anterior
- [x] 6.6 Suite de evaluación del alcance (`@Tag("llm")`, excluida por defecto y ejecutada con `./gradlew llmTest` contra cada proveedor con llave: `ANTHROPIC_API_KEY` y/o `OPENAI_API_KEY`), parametrizada por configuración: con bicicletas, unas 10 preguntas del tema → `inScope`, y unas 10 ajenas (incluida "¿Qué es un avión?") y 3 de prompt injection → la negativa configurada exacta; con "cafeteras", las de bicicletas se niegan y las de cafeteras se responden; documentar en el README cómo ejecutarla. **Verificado con OpenAI (27/27, gpt-5-mini); Anthropic queda configurado como proveedor preferido pero no se evaluó por falta de saldo en la API (decisión del usuario, 2026-09-25).**
- [x] 6.7 Implementar `FallbackBotAnswerAdapter` (Anthropic → OpenAI ante falta de llave, error o más de 12 s; sin reintento de respuestas no interpretables; espera de 30 s para el proveedor que falló), la configuración por proveedor (`max_completion_tokens` y `reasoning_effort` para OpenAI) y `OPENAI_API_KEY`/`OPENAI_MODEL` en compose; verificar con pruebas unitarias con reloj controlado y en vivo: con Anthropic sin saldo, el bot responde con OpenAI
- [x] 6.8 Registrar en cada respuesta del bot el proveedor y el modelo (`message.bot_provider`/`bot_model`, también en negativas y respuestas ilegibles, nulos en `SYSTEM`), enviarlos en el frame `MESSAGE` y mostrarlos como etiqueta en el visor del admin (no en la user-app); verificar con pruebas unitarias del caso de uso y del adaptador, una integración que los lee del frame del observador, una prueba de UI de la etiqueta y en vivo en el admin

## 7. Cliente de chat compartido (`chat-core`)

- [x] 7.1 Implementar los modelos de dominio del chat y `ConversationApi` (REST); verificar con tests de Vitest y `HttpTestingController`
- [x] 7.2 Implementar el `ChatStore` con signals (D4): mapa por `seq`, `pending` por `clientMessageId`, `lastSeq` contiguo, `botTyping`, conciliación de `MESSAGE`/`ACK` en cualquier orden y `ERROR`; verificar con tests de Vitest que un `seq` duplicado se muestra una vez, que 8 antes que 7 se ordena bien, que un pendiente se confirma con el ACK y que `TYPING` se apaga al llegar la respuesta del bot
- [x] 7.3 Implementar `ChatSocket` con estado (`connected`/`reconnecting`/`offline`), backoff con jitter (0,5 s → 10 s), reapertura con `lastSeq`, reenvío de los pendientes tras `SYNCED` (solo rol `CUSTOMER`) y cierre al cambiar de conversación; verificar con tests de Vitest usando un WebSocket falso
- [x] 7.4 Implementar la detección de huecos (esperar unos 300 ms y luego `GET /messages?afterSeq=lastSeq`); verificar con un test de Vitest que con `lastSeq=4` y la llegada de 7 se completan 5 y 6
- [x] 7.5 Implementar los componentes de UI compartidos (`message-list` con estilos por rol `CUSTOMER`/`BOT`/`SYSTEM` y marca de pendiente, `typing-indicator`, `connection-badge`) con el sistema visual de fintech; verificar con tests de componente

## 8. Apps

- [x] 8.1 `user-app` (4200): pedir el nombre, crear la conversación, guardar su id en `localStorage`, recuperarla al recargar (olvidarla si da 404), botón "Nueva conversación", lista de mensajes, indicador de "escribiendo", estado de conexión y campo de envío (máximo 2000); verificar con un test de componente y manualmente recargando la página
- [x] 8.2 `admin-app` (4300), pestaña **Conversaciones**: lista de conversaciones con polling cada 3 s (nombre, número de mensajes, remitente y extracto del último mensaje, última actividad) y visor en vivo de la conversación seleccionada como `OBSERVER`, sin campo de envío y cambiando de conversación sin recargar; verificar con un test de componente y viendo en vivo dos conversaciones de dos clientes distintos
- [x] 8.3 `admin-app`, pestaña **Alcance del bot**: formulario precargado con la configuración activa (tema, descripción, subtemas como lista editable, negativa) con la validación de D14 en el cliente y los errores 400 del servidor, guardado que muestra la versión nueva, e historial de versiones con "cargar en el formulario"; verificar con un test de componente y manualmente cambiando el tema a "cafeteras" y comprobando en la user-app que una pregunta de bicicletas recibe la negativa nueva

## 9. Documentación e integración

- [x] 9.1 Escribir `README.md`: cómo levantar (`docker compose up --build`), `.env` con `ANTHROPIC_API_KEY`/`ANTHROPIC_MODEL`, puertos (4200, 4300, 8080, 5432), conexión a Postgres y cómo correr las pruebas (incluida la suite `llm`), además de las **4 decisiones justificadas** (tiempo real, orden, reconexión y despliegue en producción) con los diagramas de `design.md`, y cómo se configura el alcance del bot (plantilla fija + campos, negativa decidida por el servidor, versiones) y sus límites; verificar que los comandos documentados funcionan tal cual
- [x] 9.2 Prueba end-to-end manual siguiendo el guion del README: user-app en dos pestañas de la misma conversación y admin-app observando; enviar desde ambas pestañas a la vez; preguntar "¿Qué es un avión?" (negativa configurada) y una pregunta de bicicletas; cambiar el tema desde la admin-app y confirmar que la siguiente respuesta usa el tema nuevo sin reiniciar; poner "Offline" en DevTools a una pestaña mientras el bot responde y volver a "Online"; confirmar que las tres vistas muestran la misma secuencia, sin duplicados ni faltantes, y que `select seq, sender_role, content from message order by seq` coincide; repetir sin `ANTHROPIC_API_KEY` y confirmar el mensaje `SYSTEM` de no disponible. **Recorrido en vivo en el navegador con OpenAI como proveedor activo (Anthropic sin saldo, decisión del usuario, 2026-09-25).**

## 10. Tomar la conversación (agente humano)

- [x] 10.1 Añadir `conversation.mode`, `agent_name` y `bot_resume_after_seq` (con `ALTER ... IF NOT EXISTS`), ampliar el check de `sender_role` con `AGENT`, y reflejarlos en modelo, entidad, mapper y DTO de conversación; verificar con Testcontainers que una base existente recibe las columnas y que la lista las devuelve
- [x] 10.2 Implementar tomar y devolver en el dominio (conflicto si la tiene otro agente, idempotente para el mismo, mensajes `SYSTEM` de aviso, evento `MODE`) y exponer `POST /api/v1/conversations/{id}/takeover` y `/release` (409 con quién la atiende); verificar con pruebas unitarias y de `WebTestClient`, incluida la toma simultánea de dos admins
- [x] 10.3 Proteger el append con guardas de modo en el mismo `UPDATE` del `seq` (`BOT` exige modo bot, `AGENT` exige modo humano y ser el agente asignado), hacer que el bot no genere nada en modo humano y respete `bot_resume_after_seq` (también en la recuperación); verificar con una integración en la que un bot lento es interrumpido por la toma (sin respuesta y sin huecos de `seq`) y con unitarias del caso de uso
- [x] 10.4 Añadir el rol `AGENT` al WebSocket (envío solo si está asignado, `ERROR NOT_ASSIGNED`), `mode`/`agentName` en `SYNCED` y el frame efímero `MODE`; verificar con integraciones de envío permitido/rechazado, del `MODE` en vivo y del modo tras reconectar
- [x] 10.5 `chat-core` y user-app: modo en el store (desde `SYNCED`/`MODE`), rol `AGENT` en la sesión, estilo de mensajes del agente y aviso "Estás hablando con <nombre> (agente de soporte)"; verificar con Vitest
- [x] 10.6 admin-app: nombre de agente recordado, conexión como `AGENT`, columna "Atiende" en la lista, botones "Tomar conversación" / "Devolver al asistente", campo de envío solo si la tiene él, y "Atendida por <nombre>" si la tiene otro; verificar con Vitest y en vivo con user-app y admin-app
- [x] 10.7 Actualizar el README (protocolo, flujo de toma, guion de prueba); verificar el guion en vivo
